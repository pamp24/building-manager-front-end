export interface CompanyDTO {
  companyId?: number;
  companyName: string;
  taxNumber: string;
  managerName: string;
  phone: string;
  email: string;
  address: string;
  addressNumber: string;
  postalCode: string;
  city: string;
  region?: string;
}
